# appendix

This section holds standalone map views and supporting material that are easier to read outside the embedded docs frame.

## full-view maps

- [Open the standalone full-view maps](/appendix/full-view-maps)

The standalone page keeps the first diagram as the top viewport and reveals the second diagram on scroll.
It also uses the detailed broad-map preset, so the Web2 revenue/receivables firms are shown individually there instead of as one aggregate placeholder.
